# App Store listing copy — 1.0 (en-US)

Date: 2026-09-13. This is the exact copy to push to App Store Connect via
`ios/scripts/asc-api.sh` for version `c5b26d42-dcd6-466f-abd4-d91d37cf6d59`
(appInfo `be42aba1-dc93-4a7a-963e-45b14437c2f4`, en-US localization
`4aeaf6b9-40c6-43c3-a89b-4f17162094cb`). Positioning follows
`docs/gtm/2026-09-03-go-to-market-brief.md`: capture/recall only, never the
investor "agent context layer" story.

## Name

**Stash -- save anything**

Will's existing choice, kept as-is per instruction. Cleaner alternative to
consider (not applied): **"Stash: save anything"** — a colon instead of a
double-hyphen reads more like a normal App Store title; Will's call.

## Subtitle (≤30 chars)

**Save anything. Ask later.**

(25 characters.)

## Promotional text (≤170 chars)

**Save links, screenshots, photos, PDFs, and voice notes from any app. Stash writes the titles, transcripts, and summaries, so you can ask for anything back.**

(155 characters.)

## Description (≤4000 chars)

```
Stash is the fastest way to save anything, and the easiest way to find it again.

Share a link, a screenshot, a photo, a PDF, or a voice note into Stash from any app on your phone. That's the whole capture flow: no naming, no filing, no folders, no decisions. One share-sheet tap and it's saved.

Once something is in your stash, it works for you. Links get real titles and descriptions. Voice notes get transcribed. Screenshots and documents get read, so their text is searchable. Long recordings and articles get summarized. Everything becomes something you can actually find later, not just something you meant to come back to.

Then you can ask for it back. Type a question in plain language — "that restaurant my friend sent me last month," "the pricing page I saved on Tuesday" — and Stash searches everything you've saved and answers with a link straight to the item. No scrolling through folders, no remembering what you called something.

What you can save:
• Links — Stash fetches the title, description, and preview image automatically
• Screenshots and photos — read and made searchable
• PDFs and documents — full text extracted and summarized
• Voice notes — transcribed automatically
• Plain notes — for the thought you don't want to lose

How you save it:
• The iOS share sheet, from Safari, Photos, or any app that can share
• The Stash app itself, for quick notes and voice memos
• Text a link or photo to Stash over WhatsApp or SMS
• The Stash web app and Chrome extension, if you also use Stash on your computer

How you find it again:
• Search across everything you've saved
• Ask Stash a question and get an answer with a link to the source item

Stash is free to download. An active Stash subscription is required to save new items.

Your stash is private by default. Nothing you save is used to train AI models, shown to other people, or sold.
```

(Character count: 1,867 of 4000.)

## Keywords (≤100 chars, comma-separated, no spaces after commas)

```
save,links,notes,bookmarks,voice memo,transcribe,pdf,screenshot,search,recall,capture,ask,share
```

(95 characters.)

## What's New in This Version (whatsNew, 1.0)

```
First release.
```

## URLs

- **Support URL:** `https://www.gostash.it`
- **Marketing URL:** `https://www.gostash.it`
- **Privacy Policy URL:** `https://www.gostash.it/privacy`

(No dedicated support page exists yet and `mailto:` links are not accepted
by ASC — do not create one; point both support and marketing at the
marketing site per the plan.)

## Categories

- **Primary:** Productivity (`PRODUCTIVITY`)
- **Secondary:** Utilities (`UTILITIES`)

## Copyright

```
2026 William Dzierson
```

## Age rating (4+)

All App Store Connect age-rating questionnaire answers: **None / False** —
no objectionable content of any kind (violence, mature themes, gambling,
horror, alcohol/tobacco/drugs, unrestricted web access as a content
category, contests, etc.), no unrestricted web access flag needed (Stash
does not embed a general-purpose browser), and no user-generated content
exposure flags beyond what "Other User Content" already covers in the App
Privacy label. Target result: **4+**.

## App Review notes (appStoreReviewDetail)

```
Stash is a personal capture-and-recall app: save links, photos, screenshots, PDFs, and voice notes, then search or ask questions across everything you've saved.

Demo account: will+review@dzierson.com
Password: see ios/.env.test.local (STASH_REVIEW_PASSWORD) — entered only into the App Store Connect "Sign-In Required" password field, not included in this note.
Contact: +1 415 583 3525

Business model: Stash is a free, stand-alone companion app to the paid Stash web service (gostash.it), offered under App Review Guideline 3.1.3(f) (free stand-alone apps as a companion to a paid web-based tool). There is no in-app purchase and no in-app subscription flow — the subscription is provisioned entirely outside the app, on the web service noted above. The app itself does not sell, unlock, or gate any digital content via Apple's in-app purchase system.

How to review:
1. Sign in with the demo account above. The View tab already has seeded content (links, a photo, a note, a voice note) so there's something to browse and ask about immediately — no need to create content first.
2. The system share sheet (from Safari, Photos, or any other app) is the app's primary capture path — try sharing a link or photo into Stash to see it land in the library.
3. The Ask tab lets you ask a question in plain language about what's saved and returns an answer with a citation linking back to the source item.
4. Location tagging and microphone/voice-note capture are both opt-in features the user turns on explicitly; neither is required to use the app.
5. Account deletion is available in Settings tab → scroll to the bottom → Delete account, and takes effect immediately.

Thank you for reviewing Stash.
```

## Decisions for Will to eyeball

- **Subtitle wording**: "Save anything. Ask later." (26/30 chars) — an
  alternative considered was "Your memory, searchable." (25/30 chars, closer
  to brief section 3's one-liner candidates) if you'd rather lead with
  recall than the capture verb.
- **Name**: kept "Stash -- save anything" per instruction; "Stash: save
  anything" is the suggested cleaner alternative, not applied.
- **Keywords**: no branded terms beyond generic category words, per ASC
  guidance against keyword-stuffing your own name; swap any of these for
  higher-intent terms if you have App Store Connect search-term data already.

## ASC state after T4b (2026-09-14)

Everything below was pushed via `ios/scripts/asc-api.sh` and confirmed with a
follow-up GET; see `docs/RELEASING.md`'s "App Store submission (1.0)" for the
full done/manual breakdown. Recorded here so this doc stays the single source
of truth for what's actually live vs. still just drafted copy above.

- **Version**: `MARKETING_VERSION` "1.0" / `CURRENT_PROJECT_VERSION` 10 in
  `ios/project.yml`, confirmed via PlistBuddy on both Info.plists after a
  local build. `appStoreVersions` GET confirms `versionString: "1.0"`. No
  build attached yet (T5's job).
- **appStoreVersionLocalizations** (`4aeaf6b9-…`): description, keywords,
  promotionalText, supportUrl, marketingUrl all live exactly as drafted
  above. `whatsNew` was rejected by ASC (`409 STATE_ERROR`, "cannot be
  edited at this time") — Apple only allows What's New starting with a
  version's second release; "First release." above is what to set once 1.1
  makes the field editable. **Re-PATCHed 2026-09-14** (whole-branch review
  finding A): the description's closing sentence originally read "an active
  Stash subscription from gostash.it is needed to save new items. Manage
  your subscription anytime at gostash.it." — steering language naming an
  external purchase/management destination — cut to the current "An active
  Stash subscription is required to save new items." and GET-verified to
  contain neither "gostash.it" nor "Manage your subscription".
- **appInfoLocalizations** (`6efedb1c-2a3e-4571-8f0a-1a52cdb56f9d`):
  subtitle "Save anything. Ask later." and privacyPolicyUrl live; name
  unchanged ("Stash -- save anything").
- **appInfos** (`be42aba1-…`): primaryCategory `PRODUCTIVITY`, secondaryCategory
  `UTILITIES`, both confirmed via GET on the relationship endpoints.
- **appStoreVersions** (`c5b26d42-…`): copyright "2026 William Dzierson" live.
- **ageRatingDeclarations** (id `be42aba1-…`, same as the appInfo id): all
  fields set to their NONE/false default. Two type gotchas for next time:
  `gunsOrOtherWeapons` is the NONE/INFREQUENT_OR_MILD/FREQUENT_OR_INTENSE
  enum (not boolean), `healthOrWellnessTopics` is boolean (not that enum);
  `lootBox` is a required field the API doesn't surface until you omit it
  and get a `409 ENTITY_ERROR.ATTRIBUTE.REQUIRED`.
- **appStoreReviewDetails** (`84cae442-05a9-4621-8f23-d87923061db5`,
  created — none existed before): contact info, demo account name, notes
  all live verbatim per the section above; `demoAccountPassword` is set
  from `ios/.env.test.local`'s `STASH_REVIEW_PASSWORD` (never printed or
  committed). **Re-PATCHed 2026-09-14** (whole-branch review findings A/B):
  the Business-model paragraph's second `gostash.it` mention ("subscriptions
  are created and managed entirely on gostash.it, outside the app") was
  reworded to "the subscription is provisioned entirely outside the app, on
  the web service noted above" (the first mention, identifying the
  companion service, stays — required for the 3.1.3(f) justification); step
  5's path was corrected from "Settings → Your Information → Delete
  account" (that section doesn't exist) to "Settings tab → scroll to the
  bottom → Delete account." Both changes GET-verified in the live `notes`
  field.
- **Screenshots** (6.9" set `3e44a2e8-ca07-4b9d-bf93-90c5b3c58d30`): all six
  frames uploaded and `assetDeliveryState.state == COMPLETE`, in order:
  `01-view-library.png`, `02-detail-link.png`, `03-add-composer.png`,
  `04-ask-answer.png`, `05-share-compose.png`, `06-share-tutorial-panel2.png`
  (all exactly 1320×2868, all ≤1.5MB — 03 and the initial 01/02/06 needed a
  lossless PNG re-save, `PIL` `optimize=True`/`compress_level=9`, confirmed
  pixel-identical, to clear the limit — PNGs live in
  `docs/app-store/screenshots/1.0/`). One correction to this plan's own
  assumption: ASC's `screenshotDisplayType` enum has no `APP_IPHONE_69`
  value (the live API rejects it and lists the accepted set) — the
  6.9"/1320×2868 class is still addressed as `APP_IPHONE_67` on this API
  version, so that's the value the set above actually uses.
  **Reshot 2026-09-14** (coordinator follow-up + whole-branch review finding
  C), all six replaced (old assets deleted, new ones uploaded, ordering
  re-PATCHed via `relationships/appScreenshots` and re-verified — current
  ids: `ed5a2883-…` (01), `f2ce8bac-…` (02), `b8d05b4c-…` (03),
  `db98a01c-…` (04), `fd86e7b3-…` (05), `59289206-…` (06)):
  - The review account's voice-note item (`1951761a-…m4a`, raw UUID
    filename title, "no speech was detected" — the sim has no mic) was
    deleted via REST (verified by id AND title before deleting; the photo,
    3 links, and note were left untouched) so 01 no longer leads with it —
    the mountain photo now sorts first, with the note and an enriched link
    (real og-image) visible below it.
  - 05 now shares `https://www.nasa.gov` (already seeded, so this capture
    cancels rather than saves) instead of `example.com`, for a recognizable
    URL in the compose card.
  - Status bar reshot with `--batteryState discharging --batteryLevel 100`
    (a solid, non-charging battery glyph) for a consistent set.
  - 03 and 04 now dismiss the keyboard before capturing. 03 uses the
    composer's own `capture.dismissKeyboard` control. 04 needed five
    iterations to find a working approach — `ChatComposerBar`'s `TextField`
    has no focus-management to resign via, a keyboard swipe gesture once
    popped a system "Enable Dictation?" prompt and backgrounded the app, a
    manual key-by-key drag typed garbage into the field, and neither a tab
    hop nor an `ask.history` push/pop round trip actually dropped focus
    (`@FocusState` is bound at the `AskView` struct level, which
    NavigationStack/TabView keep alive, so it silently regains focus the
    instant the view reappears) — what worked was relaunching the app after
    asking: `ChatHistoryAPI.loadHistory`'s own doc comment confirms a
    reloaded conversation still gets a citation-chip stand-in (fallback
    title "Item N") and the inline `[1]` is already baked into the
    persisted content, so the restored bubble is not actually sourceless,
    contradicting an older assumption elsewhere in this codebase.
- **App Privacy**: `docs/app-store/2026-09-13-app-privacy-answers.md` and
  both `PrivacyInfo.xcprivacy` manifests (`ios/Stash/`,
  `ios/StashShareExtension/`) now also declare **Search History**
  (collected, linked to identity, App Functionality only, not tracking) —
  the Ask tab's questions/answers persist server-side as conversation
  history (visible via the history icon in the Ask header), which is
  in-app ask/search history, not web browsing history. Not push-able via
  the API; picked up automatically the next time these files ship in a
  build.
- **Not done by design**: attaching a build to this version record, App
  Privacy nutrition-label answers (not settable via this API), and the
  Submit-for-Review click — see `docs/RELEASING.md` for exactly who does
  each of those and when.
