# Stash iOS Plan 14: Housekeeping Mirror + App Store Readiness Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Mirror the 2026-09-13 web housekeeping changes in the native app (card notes, standalone Notes, enrichment status, speaker transcripts, Montreal card headings, editor footprint) and close every engineering gap that stands between the current TestFlight build and an App Store 1.0 submission this week (account deletion, privacy manifests, phone storage bug, paywall-403 handling, 1.0 metadata + screenshots, build 10).

**Architecture:** Four file-disjoint implementation tasks, two at a time (max 2 concurrent builders): T1 Library/card notes + typography, T3 Settings/account deletion + phone + Outbox 403 (round 1); T2 Detail transcribe-with-speakers + editor footprint, T4 release readiness (manifests, version 1.0, ASC metadata, store screenshots) (round 2). T5 wrap = suites ×2, build 10, attach to TestFlight groups AND to the 1.0 App Store version record; submission for App Review itself is Will's click after the blockers below are cleared.

**Tech Stack:** SwiftUI iOS 17, StashKit (SPM), XCUITest, XcodeGen, `ios/scripts/release.sh`, `ios/scripts/asc-api.sh` (App Store Connect REST, ES256 JWT), Supabase edge functions already deployed (`delete-account`, `transcribe-audio`).

**Spec:** `docs/2026-09-13-housekeeping-handoff.md` (web changes + "Follow-up for the mobile agent" + "Native follow-up" paragraphs), `docs/testflight-readiness-audit-2026-09-05.md` (H1, H2, H4, H5, M4), `docs/gtm/2026-09-07-v1-launch-punch-list.md` (A8 phone bug, iOS track), DESIGN.md, `docs/ETHOS.md`, `docs/PLATFORM_API.md`.

## Global Constraints

- **Base commit carries the housekeeping agent's uncommitted native work** (Notes section above source tabs, `contentTabsConfig` without `.notes`, type chip beside the date, 5-line clamps, `ItemAttributes.enrichmentStatus(at:)` + `TimelineView` dimming/status pill, `EnrichmentStateTests`). Build on it; do not re-derive it.
- **Server state to assume:** `attributes.enrichment` is written only once the web agent's rollout (migration `20260913120000_item_enrichment_state.sql` + `add-url`/`add-file`/`transcribe-audio` redeploys) happens — NOT done as of plan start. iOS must degrade gracefully (no key → no dimming), which the carried code already does. `transcribe-audio` currently deployed = whisper-1 `{ audioUrl, fileName } → { transcription, description }`; the diarized version keeps the same request/response shape (Markdown speaker turns in `transcription`). `delete-account` IS deployed: `POST` with the user's `Authorization: Bearer <access token>`, no body → `200 { deleted: true, storageObjects, stripe }` / `401` / `403` (agent token) / `500 { deleted: false, error }`; the DB cascades; Stripe subscription is cancelled server-side.
- **Design rules:** DESIGN.md tokens only; 1px strokes; no emoji; light-only lock; PP Neue Montreal everywhere except the detail-sheet title (PP Editorial New stays there). NEW per handoff: card headings = Montreal **medium 20pt, tracking −0.014em** (`StashType.cardTitle()` — add the token to StashType.swift AND DESIGN.md in the same commit; `editorialTitle` remains for the detail sheet). Cards are natural height with **24pt vertical gutters** between cards on the phone's single column (no masonry on phone).
- **Card notes (handoff follow-up 1):** empty-notes cards show an explicit "Add a note" affordance (muted text button with a `plus` glyph, in the old chip area, identifier `card.addNote`); existing notes are tappable (identifier `card.note`) and render with a straight, square-ended 2pt-wide **violet600 rule along the left edge** (a filled rule, not a stroke — the 1px rule is for strokes), only the right corners of the hover surface rounded; five-line truncation outside editing. Tapping opens a compact **editor sheet** (`CardNoteEditorSheet`, `.presentationDetents([.medium])`) reusing `NotesEditorModel` + the item store's patch/merge flow + draft recovery — never flatten rich `content` to plain text (TipTap doc round-trip via the existing `TipTapAppend`/notes-editor path). Explicit Save / Cancel buttons; native Return inserts a newline; Cancel discards; unchanged Save just closes. After a confirmed save: a 450ms lavender wash (violet300 @ 0.25 → 0) over the note plus a brief `checkmark` + "Saved" acknowledgment (2s), static under Reduce Motion; failure keeps the draft in the sheet and shows the error inline. Whole-card tap must still open the detail sheet everywhere except the note/add-note hit areas (check gesture precedence); VoiceOver: note = button "Edit note", add = button "Add a note".
- **Transcribe with speakers (handoff follow-up 2):** in `ItemDetailContent`'s Transcript section header trailing slot, a text button "Transcribe with speakers" (identifier `detail.transcribeSpeakers`, audio/video items with a stored media file only). Flow mirrors `src/components/TranscriptContent.tsx`: resolve the stored media URL the same way the detail player does → `StashClient.functions.invoke("transcribe-audio", body: {audioUrl, fileName})` → PATCH only `page_body` + `description` via a new `StashKit.TranscriptionService` (unit-tested with a stubbed client) → refresh embeddings (`EmbeddingRefresher`) → refresh the item store. Previous transcript preserved on failure; `content` never touched; copy never claims real names ("Speaker 1/2…" comes from the server). Busy state disables the button and shows "Transcribing…"; errors surface inline under the header.
- **Notes editor footprint (handoff):** `NotesEditor` frame `minHeight: 80, maxHeight: 220` → `minHeight: 44, maxHeight: 110` (≈half), scrollable inside, grows with Dynamic Type (use `@ScaledMetric`), keyboard stays visible. Card-note sheet uses the same editor.
- **Account deletion (5.1.1(v)):** Settings gets a `DeleteAccountSection` after Sign Out: row "Delete account" (destructive, identifier `settings.deleteAccount`) → sheet explaining consequences (everything you saved, your phone number, your subscription — copy mirrors `src/components/settings/DeleteAccountSection.tsx`) with a "Type DELETE to confirm" field and a "Delete everything" destructive button enabled only on exact match (identifier `settings.deleteAccount.confirm`). On success: purge local state (Outbox for that user, staged files, App Group defaults incl. `subscription.canAddContent`, keychain session via the existing sign-out path) and land on the sign-in screen with a one-line "Your account was deleted." banner. Failure: inline error, account intact. `StashKit.AccountDeleter` with unit tests (200 / 401 / 500 bodies). UI test `testDeleteAccountEndToEnd` (new file `ios/StashUITests/AccountUITests.swift`, self-contained sign-up helper): sign up a throwaway `will+del-<timestamp>@dzierson.com`, delete it, assert the sign-in screen + a REST probe that sign-in now fails. Never run it against the fixture or review accounts.
- **Phone storage bug (punch list A8):** iOS sign-up and the Settings phone section must store `user_phone_numbers.phone_number` as bare-digit E.164-without-plus exactly like web `src/utils/phoneNumber.ts` (US: 10 digits → prepend `1`; leading `1` + 10 digits kept; anything else rejected with the same copy). Port `formatPhoneNumber` to StashKit `PhoneNumber.swift` with tests (parity cases from the web test file), use it in `SessionStore.signUp` and `PhoneSection`.
- **Outbox park-on-403:** when a capture drain gets HTTP 403 with `{"error":"subscription_required"}`, the entry is **parked** (new state, not retried, attempts not burned) and the Add tab shows the existing gate strip; parked entries drain automatically on the next successful `SubscriptionStore` refresh that reports `canAddContent == true`. StashKit tests.
- **Privacy manifests (audit H1):** `ios/Stash/PrivacyInfo.xcprivacy` + `ios/StashShareExtension/PrivacyInfo.xcprivacy`, declared as resources in `project.yml`. `NSPrivacyTracking` false; `NSPrivacyAccessedAPITypes`: UserDefaults `CA92.1`, file timestamp `C617.1` (Outbox/StagedFileStore read modification dates — verify with grep and drop if unused), disk space `E174.1` only if used, system boot time only if used. `NSPrivacyCollectedDataTypes` (linked to identity, not used for tracking): email address, phone number, user content (photos/videos, audio, other user content), precise location (app-functionality, opt-in), user ID. Verify the archive's generated privacy report (`xcodebuild -exportArchive` privacy report or `PrivacyReport` in Organizer equivalent) lists no undeclared required-reason APIs.
- **Version:** `MARKETING_VERSION` `0.1.0` → `1.0` (must equal the ASC version record string "1.0"), `CURRENT_PROJECT_VERSION` → 10.
- **App Store metadata (ASC version `c5b26d42-dcd6-466f-abd4-d91d37cf6d59`, appInfo `be42aba1-dc93-4a7a-963e-45b14437c2f4`, en-US localization `4aeaf6b9-40c6-43c3-a89b-4f17162094cb`):** write via `asc-api.sh` PATCH/POST: name stays "Stash -- save anything" unless Will changes it; subtitle "Save anything. Find it later."; description + keywords + promotional text + whatsNew drafted from `docs/gtm/2026-09-03-go-to-market-brief.md` positioning (capture/recall story only — never the agent-context-layer story); supportUrl and marketingUrl both `https://www.gostash.it` (no support page exists yet and `mailto:` is not accepted — create nothing on the web), privacyPolicyUrl `https://www.gostash.it/privacy`; primary category Productivity (`PRODUCTIVITY`), secondary Utilities; copyright "2026 William Dzierson"; age rating declaration all "NONE"/false (4+); release type AFTER_APPROVAL (already). Review notes (`appStoreReviewDetail`): demo account will+review@dzierson.com + password from `ios/.env.test.local` (STASH_REVIEW_PASSWORD, written ONLY into the ASC field, never into docs), contact phone +14155833525, notes explaining: free companion app to the paid web service under 3.1.3(f), no IAP, subscription managed on gostash.it, share extension is the primary capture path, Sign in → View tab has seeded content. **App Privacy nutrition labels cannot be set via the API** — produce `docs/app-store/2026-09-13-app-privacy-answers.md` with the exact answers for Will to click.
- **Store screenshots:** 6.9" set only (screenshotDisplayType `APP_IPHONE_69`, 1320×2868 portrait PNG). Capture on the **iPhone 17 Pro Max** sim (`3F61D023-8A5F-4EB5-A0A3-83213A756EAA`) signed in as the review account, after seeding it with realistic content (3 links to well-known public pages with og-images, 1 photo, 1 short note, 1 voice note — all created through the app's own capture path so enrichment runs; keep them, they double as reviewer content). Six frames: View library, detail sheet (link), Add tab, Ask with an answer + citation, share sheet (Safari → Stash), share tutorial panel 2. Plain device frames, no marketing captions (ship fast; captions are a later polish). Upload via the ASC API (appScreenshotSets → appScreenshots reserve → PUT chunks → commit with md5) — extend `asc-api.sh` with an `upload-screenshot <setId> <png>` subcommand; verify each reaches `COMPLETE`. Keep the PNGs in `docs/app-store/screenshots/1.0/` (≤1.5MB each).
- **Removed banner check:** the native app has no recurring paste/drop tutorial banner (only the Ask composer placeholder) — nothing to remove; record that in the outcome.
- **Process:** sims `28F9E3CD-90E2-4D17-AFDE-D0C37316BFBB` (T1/T2) and `46D4EA93-94D5-451E-AC61-A5485AFB211F` (T3/T4); `-derivedDataPath DerivedData`; EXPORTED TEST_RUNNER_* env from ios/.env.test.local; single writer per file per round (`StashUITests.swift`: T1 in round 1, T2 in round 2; T3 and T4 use NEW test files); `xcodegen generate` after adding files; builds warning-free; StashKit floor 346 (344 + the 2 carried EnrichmentState tests); UI suite 24 + new, standing 3 gate-blocked failures (testCaptureSmoke/testLocationPinSmoke/testAskSmoke) until Stripe comps land; never touch Stripe; never print credentials; UITEST-FIXTURE rows permanent; the review account's items are permanent once seeded.

---

### Task 1: Card notes + Montreal card headings + gutters (Library)

**Files:** Modify `ios/Stash/Library/ItemCardView.swift`, `ios/Stash/Library/LibraryView.swift`, `ios/Stash/Library/CardBits`/`CardChips.swift` as needed, `ios/Stash/Design/StashType.swift`, `DESIGN.md`; Create `ios/Stash/Library/CardNoteEditorSheet.swift`; Modify `ios/StashUITests/StashUITests.swift` (append `testCardNoteAddEditAndSaveAcknowledgment`, update any title-font assertions).

- [ ] `StashType.cardTitle()` token (Montreal medium 20, tracking −0.014em) + DESIGN.md entry; cards use it (detail keeps `editorialTitle`).
- [ ] Natural-height cards, 24pt gutters in `LibraryView`'s list (read current spacing; record before/after).
- [ ] "Add a note" affordance + tappable note with the left violet rule; `CardNoteEditorSheet` on `NotesEditorModel`; save acknowledgment + failure path; whole-card tap precedence verified; VoiceOver labels.
- [ ] Screenshots (empty-notes card, note card, sheet, saved wash) → `.superpowers/sdd/plan-14/task-1-*.png`; READ them.
- [ ] UI test appended; smokes: `testCardAnatomySmoke`, `testLibrarySmoke`, `testDetailSheets`, new test ×2.
- [ ] Commit `feat(ios): plan-14 T1 — card notes editor, Montreal card headings, 24pt gutters`.

### Task 3: Account deletion + phone E.164 + Outbox park-on-403 (Settings/Auth/StashKit)

**Files:** Create `ios/Stash/Settings/DeleteAccountSection.swift`, `ios/StashKit/Sources/StashKit/AccountDeleter.swift`, `ios/StashKit/Sources/StashKit/PhoneNumber.swift`, tests for both, `ios/StashUITests/AccountUITests.swift`; Modify `ios/Stash/Settings/SettingsView.swift`, `ios/Stash/Settings/PhoneSection.swift`, `ios/Stash/Auth/SessionStore.swift`, `ios/Stash/Auth/SignInView.swift` (deleted banner), `ios/StashKit/Sources/StashKit/Outbox.swift` (+ `CaptureAPI.swift` error typing), `ios/Stash/Capture/*` only where the gate strip reads parked state.

- [ ] `PhoneNumber.normalize` + parity tests; wire sign-up + Settings.
- [ ] `AccountDeleter` + tests; `DeleteAccountSection` UI; local purge; deleted banner.
- [ ] Outbox parked state + drain-on-entitlement + tests.
- [ ] `testDeleteAccountEndToEnd` (throwaway account) ×2 green; `testSettingsSmoke`, `testCaptureSmoke` (expected gate-blocked — confirm it now parks instead of burning attempts).
- [ ] Commit `feat(ios): plan-14 T3 — account deletion, E.164 phone storage, Outbox parks on subscription 403`.

### Task 2: Transcribe with speakers + editor footprint (Detail)

**Files:** Create `ios/StashKit/Sources/StashKit/TranscriptionService.swift` + tests; Modify `ios/Stash/Detail/ItemDetailContent.swift`, `ios/Stash/Detail/NotesEditor.swift`, `ios/Stash/Detail/ItemDetailView.swift`; Modify `ios/StashUITests/StashUITests.swift` (append `testTranscribeWithSpeakersButtonAndFootprint`).

- [ ] `TranscriptionService` (invoke → patch page_body/description → embeddings refresh) + tests with stubbed responses incl. failure preserving the old transcript.
- [ ] Button in the Transcript header; busy/error states; item store refresh.
- [ ] Notes editor half height with `@ScaledMetric`; verify with keyboard open, a long rich note, and XXL Dynamic Type (screenshots).
- [ ] Run the real flow ONCE against the `UITEST-FIXTURE: audio one` fixture? NO — fixtures are permanent and their transcript must not change. Use a throwaway voice note recorded by the test (`testVoiceNoteSmoke` recipe) then delete it.
- [ ] Commit `feat(ios): plan-14 T2 — transcribe with speakers, half-height notes editor`.

### Task 4: Release readiness (manifests, 1.0, ASC metadata, screenshots)

**Files:** Create `ios/Stash/PrivacyInfo.xcprivacy`, `ios/StashShareExtension/PrivacyInfo.xcprivacy`, `docs/app-store/2026-09-13-app-privacy-answers.md`, `docs/app-store/2026-09-13-listing.md` (the exact copy pushed to ASC), `docs/app-store/screenshots/1.0/*.png`, `ios/StashUITests/StoreScreenshotsUITests.swift` (skips unless `STORE_SCREENSHOTS=1`); Modify `ios/project.yml`, `ios/scripts/asc-api.sh` (screenshot upload), `docs/RELEASING.md` (App Store section).

- [ ] Manifests + project.yml resources; build both targets; archive privacy report check.
- [ ] Version 1.0 / build 10 in project.yml → xcodegen → plists.
- [ ] Seed the review account through the app (6 items), capture 6 frames on the 17 Pro Max sim, upload to the 6.9" set, verify COMPLETE.
- [ ] PATCH metadata, appInfo localization, categories, age rating, review detail (demo creds into ASC only).
- [ ] Docs: listing copy, privacy answers for Will, RELEASING App Store section (submission = attach build → Will clicks Submit after nutrition labels).
- [ ] Commit `chore(ios): plan-14 T4 — privacy manifests, 1.0 metadata + screenshots, build 10 prep`.

### Task 5: Wrap

- [ ] Merge origin/main (expect the housekeeping web commit — iOS hunks identical to the carried base; resolve by keeping ours where they differ only in comments).
- [ ] `docs/ui-changes.md` entry "2026-09-13 · iOS housekeeping mirror + App Store readiness (plan 14)"; plan Outcome.
- [ ] Suites: StashKit, `npm test`, UI ×2 (expect the standing 3 unless comps landed).
- [ ] `release.sh all` → upload build 10 → VALID → attach both TestFlight groups → attach to App Store version 1.0 (`PATCH /v1/appStoreVersions/{id}/relationships/build`) → beta review submit for build 10 if nothing is in review. Do NOT create an App Store review submission — that is Will's click.
